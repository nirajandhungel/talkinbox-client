# Frontend environment variables
# All VITE_ prefixed variables are exposed to the browser bundle.

# Base URL for the backend API.
# Leave empty in development — the Vite proxy handles /api/* → localhost:3000.
# In production via Docker, Nginx routes /api → backend, so leave empty there too.
# Only set this if your API lives on a different domain.
VITE_API_URL=
